# Run-PSADT-Packages

## Description
Run the PSAppDeployToolkit `Invoke-AppDeployToolkit.exe` installer/uninstaller for each package under `!WorkingDir` or `!Ready`.

## Script
`Run-PSADT-Packages.ps1`
