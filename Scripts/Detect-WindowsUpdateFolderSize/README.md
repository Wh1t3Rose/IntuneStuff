# Detect-WindowsUpdateFolderSize

## Scripts
- `Detect-WindowsUpdateFolderSize.ps1`: run on T2 machine after syncing SharePoint or after you place a zip on a web server:.

## Usage
Run in PowerShell with the required permissions and dependencies.

## Script Notes
### `Detect-WindowsUpdateFolderSize.ps1`
- Synopsis: run on T2 machine after syncing SharePoint or after you place a zip on a web server:.
